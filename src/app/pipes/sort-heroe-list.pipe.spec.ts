import { SortHeroeListPipe } from './sort-heroe-list.pipe';

describe('SortHeroeListPipe', () => {
  it('create an instance', () => {
    const pipe = new SortHeroeListPipe();
    expect(pipe).toBeTruthy();
  });
});
